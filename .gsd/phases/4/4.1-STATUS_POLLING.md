---
phase: 4
plan: 1
wave: 1
depends_on: []
files_modified:
  - app/services/api/entries/index.server.ts
  - app/routes/projects.$id._index.tsx
  - app/components/EntriesTable/index.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Users can click 'Analyze' to trigger a mock async prediction"
    - "Status badges correctly reflect 'processing' and then 'analyzed'"
  artifacts:
    - "Service layer updated to handle status updates"
---

# Plan 4.1: Async State & Status Polling

<objective>
Implement the mechanism to trigger predictions and visualize the "Processing" state.
Purpose: To bridge the gap between static entries and analysis results.
Output: Service-layer handlers for async tasks and UI status updates.
</objective>

<context>
- .gsd/SPEC.md
- app/services/api/entries/index.server.ts
- app/components/EntriesTable/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Update service layer for async predictions</name>
  <files>app/services/api/entries/index.server.ts, app/services/api/entries/types.ts</files>
  <action>
    Add a `triggerPrediction(projectId: number, entryIds: string[])` function that returns a TaskId.
    Mock the async behavior by updating state in the BFF/Mock store after a 2-second delay.
    Ensure `getEntries` returns the updated status (`processing` -> `analyzed`).
    AVOID: Complex threading since this is a mock frontend. Use `setTimeout` within the mock layer to simulate latency.
  </action>
  <verify>Check service layer logs or use a small script to verify status transitions</verify>
  <done>Predicting on entry IDs triggers status change in the mock store</done>
</task>

<task type="auto">
  <name>Implement "Analyze" button & badge logic</name>
  <files>app/routes/projects.$id._index.tsx, app/components/EntriesTable/index.tsx</files>
  <action>
    Add a main "Start Analysis" button in the project view.
    Update the `EntriesTable` to show "Analyze" for entries with `done/uploaded` status and a loading spinner for `processing`.
    Trigger a `submit` to the action when starting analysis.
    AVOID: Multiple triggers for the same entry. Disable button during processing.
  </action>
  <verify>Button click initiates analysis, badge changes to 'processing', then 'analyzed' after refresh/poll</verify>
  <done>UI reflects prediction state lifecycle</done>
</task>

</tasks>

<verification>
 After all tasks, verify:
- [ ] Clicking "Analyze" on an entry changes its badge to "processing"
- [ ] After processing completes (mocked), badge shows "analyzed"
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
- [ ] Prediction lifecycle (trigger -> process -> done) is visually represented
- [ ] API mock correctly manages state transitions
</success_criteria>
