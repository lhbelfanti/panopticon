---
phase: 4
plan: 3
wave: 3
depends_on: ["4.1"]
files_modified:
  - package.json
  - app/components/Modals/StatisticsModal.tsx
  - app/routes/projects.$id._index.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Chart visualization is rendered for analyzed project data"
    - "Stats modal is accessible from the project view dashboard"
  artifacts:
    - "StatisticsModal.tsx component renders recharts"
---

# Plan 4.3: Project Statistics Dashboard

<objective>
Implement a project statistics dashboad using charts.
Purpose: Visualize the distribution of Adverse Human Behaviours across the project corpus.
Output: Real-time charts for high-level data insights.
</objective>

<context>
- .gsd/SPEC.md
- app/routes/projects.$id._index.tsx
</context>

<tasks>

<task type="auto">
  <name>Install recharts</name>
  <files>package.json</files>
  <action>
    Install `recharts` using `npm install recharts`.
    Use `--save` to ensure it is in dependencies.
    AVOID: Complex config for now; standard import should work with Vite.
  </action>
  <verify>Check package.json for recharts</verify>
  <done>recharts is installed and available</done>
</task>

<task type="auto">
  <name>Implement StatisticsModal with Charts</name>
  <files>app/components/Modals/StatisticsModal.tsx</files>
  <action>
    Create a modal that renders a BarChart or PieChart showing behavior distribution.
    Use dummy chart data derived from the project's analysis results.
    Apply the project's color palette (e.g., `--bittersweet-shimmer` for highlights).
    AVOID: Heavy charts on initial render. Consider using `ResponsiveContainer` for better fit.
  </action>
  <verify>Render the modal to check chart display and responsiveness</verify>
  <done>StatisticsModal exists and correctly renders charts</done>
</task>

<task type="auto">
  <name>Integrate Statistics action in Project View</name>
  <files>app/routes/projects.$id._index.tsx</files>
  <action>
    Add a "Project Statistics" button in the project dashboard header (near the Analyze button).
    Trigger the modal when clicked.
    Ensure it only shows aggregate data for already analyzed entries.
    AVOID: Misleading data when no entries are analyzed yet. Show a 'no data' state if needed.
  </action>
  <verify>Clicking 'Stats' opens the modal with visualized analytics</verify>
  <done>Charts are integrated into the project workflow</done>
</task>

</tasks>

<verification>
 After all tasks, verify:
- [ ] Charts are visible and responsive
- [ ] Data distribution matches the analyzes results
</verification>

<success_criteria>
- [ ] Researchers can view high-level project analytics
- [ ] The dashboard looks professional and consistent with the design
</success_criteria>
