---
phase: 4
plan: 3
wave: 3
depends_on: ["4.1", "4.2"]
files_modified:
  - app/components/Modals/AnalysisReportModal.tsx
  - app/components/Analysis/AnalysisHistoryTable.tsx
  - app/utils/pdfGenerator.ts
  - package.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Clicking 'View Report' on a finished analysis shows a visual story with charts"
    - "Clicking 'Download PDF' generates a formatted document on-the-fly"
    - "PDF Generation is performed asynchronously on the frontend"
  artifacts:
    - "AnalysisReportModal.tsx component renders charts"
    - "pdfGenerator.ts (jspdf) utility"
---

# Plan 4.3: Analysis Story & PDF Reporting

<objective>
Implement the visual report modal and the asynchronous PDF generation system.
Purpose: To tell a data-driven story of the investigation results.
Output: A polished analysis report modal and a PDF export feature.
</objective>

<context>
- .gsd/ROADMAP.md
- app/components/Analysis/AnalysisHistoryTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Install plotting and PDF libraries</name>
  <files>package.json</files>
  <action>
    Install `recharts` for plotting.
    Install `jspdf` and `jspdf-autotable` (or similar) for PDF generation.
    Ensure components use these in a responsive way.
  </action>
  <verify>Check package.json for new dependencies</verify>
  <done>recharts and jspdf are ready to use</done>
</task>

<task type="auto">
  <name>Implement Analysis Report Story</name>
  <files>app/components/Modals/AnalysisReportModal.tsx</files>
  <action>
    Create a modal that renders multiple charts (BarChart for behavior distributions, PieChart for confidence).
    Incorporate textual summaries that highlight findings (e.g., "Top detected behavior: Sexism").
    Create a clear narrative flow using typography and colors (`--bittersweet-shimmer`).
    AVOID: Overwhelming the user with raw numbers. Use visualizations to tell a story.
  </action>
  <verify>Check layout consistency and responsiveness in the preview</verify>
  <done>Visual story modal is complete and matches premium aesthetic</done>
</task>

<task type="auto">
  <name>Implement Async PDF Logic</name>
  <files>app/utils/pdfGenerator.ts</files>
  <action>
    Implement `generateAnalysisPDF(analysisRun)` using `jspdf`.
    Include the project metadata, run results, and a visual representation of findings.
    Fetch subproject data to build the report dynamically.
    AVOID: Synchronous blocking. Ensure PDF generation doesn't freeze the UI.
  </action>
  <verify>Clicking 'Download PDF' triggers a file download in the browser with correctly formatted content.</verify>
  <done>Asynchronous PDF generation system is fully implemented</done>
</task>

</tasks>

<verification>
 After all tasks, verify:
- [ ] Clicking "View Report" shows charts.
- [ ] Clicking "Download PDF" generates a file on-the-fly.
- [ ] PDFs and Modals tell a consistent and compelling story.
</verification>

<success_criteria>
- [ ] Researchers can view and download high-quality investigation reports.
- [ ] All analysis data is professionaly presented and accessible from history.
</success_criteria>
