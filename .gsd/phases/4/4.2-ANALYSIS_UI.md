---
phase: 4
plan: 2
wave: 2
depends_on: ["4.1"]
files_modified:
  - app/routes/projects.$id.subprojects.$subId.analysis.tsx
  - app/components/Analysis/AnalysisHistoryTable.tsx
  - app/components/Analysis/NewAnalysisForm.tsx
  - app/components/EntriesTable/index.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Analysis screen is accessible via a dedicated route"
    - "New Analysis tab allows choosing exclusions and triggering a run"
    - "EntriesTable supports an 'Exclude entries' mode with checkboxes"
  artifacts:
    - "AnalysisHistoryTable.tsx, NewAnalysisForm.tsx exist"
---

# Plan 4.2: Analysis UI & Entry Exclusions

<objective>
Implement the dedicated Analysis screen and the exclusion selection logic in the EntriesTable.
Purpose: To provide a clean workspace for configuring and tracking investigation runs.
Output: A new Analysis route with tabs and an upgraded EntriesTable for selection.
</objective>

<context>
- .gsd/ROADMAP.md
- .gsd/DECISIONS.md
- app/components/EntriesTable/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Implement 'Exclude entries' mode in EntriesTable</name>
  <files>app/components/EntriesTable/index.tsx</files>
  <action>
    Add an 'Exclude entries' toggle button in the table toolbar.
    When active, show a checkbox column at the start of the table.
    All checkboxes should be selected by default (meaning 'included').
    Implement state to track unselected (excluded) entry IDs.
    Ensure existing filters correctly filter the visible entries for easier selection/deselection.
    AVOID: Persisting exclusions globally; keep it localized to the current view or pass it to the 'New Analysis' form.
  </action>
  <verify>Toggle button shows/hides checkbox column. Unchecking entries updates the exclusion state.</verify>
  <done>Exclusion mode is integrated into the EntriesTable</done>
</task>

<task type="auto">
  <name>Create Analysis Route and Tabs</name>
  <files>app/routes/projects.$id.subprojects.$subId.analysis.tsx</files>
  <action>
    Create a new route for subproject analysis.
    Implement two main tabs/sections: 'New analysis' and 'History'.
    'New analysis' should contain the `NewAnalysisForm` (trigger button + summary of exclusions).
    'History' should contain the `AnalysisHistoryTable`.
    AVOID: Simple one-page layout. Use clear visual separation between 'Create' and 'History' as requested.
  </action>
  <verify>Navigation to /analysis route works. Tabs switch correctly.</verify>
  <done>Analysis management screen structure is complete</done>
</task>

<task type="auto">
  <name>Implement Analysis UI components</name>
  <files>app/components/Analysis/AnalysisHistoryTable.tsx, app/components/Analysis/NewAnalysisForm.tsx</files>
  <action>
    `NewAnalysisForm`: Show a summary of how many entries are included/excluded. Add a 'Generate Analysis' button.
    `AnalysisHistoryTable`: List past runs with ID, Date, Exceptions count, Status, and Actions (View/Download).
    Follow the premium dark theme with consistent Lucide icons.
  </action>
  <verify>Components render correctly with mock data</verify>
  <done>UI components for analysis management are implemented</done>
</task>

</tasks>

<verification>
 After all tasks, verify:
- [ ] Researchers can exclude entries from the table view.
- [ ] New analysis runs appear in history with 'processing' status.
- [ ] The interface follows the tabbed structure.
</verification>

<success_criteria>
- [ ] Analysis workflow from selection -> triggering -> history is fully implemented.
- [ ] UI is intuitive and integrated with existing table filters.
</success_criteria>
