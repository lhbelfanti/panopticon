---
phase: 4
plan: 1
wave: 1
depends_on: []
files_modified:
  - app/services/api/analysis/types.ts
  - app/services/api/analysis/index.server.ts
  - app/services/api/mock/store.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Analysis system supports concurrent runs per subproject"
    - "History of analysis runs is persisted in the mock store"
    - "Excluded entry IDs can be passed and persisted in an analysis run"
  artifacts:
    - "analysis.server.ts service exists"
---

# Plan 4.1: Subproject Analysis Infrastructure

<objective>
Build the core service layer for managing Subproject Analysis runs and their history.
Purpose: To allow researchers a traceable, repeatable way to derive insights from predictions.
Output: Types, service handlers, and a modified state store.
</objective>

<context>
- .gsd/SPEC.md
- .gsd/DECISIONS.md
- app/services/api/entries/types.ts
</context>

<tasks>

<task type="auto">
  <name>Define Analysis types and schema</name>
  <files>app/services/api/analysis/types.ts</files>
  <action>
    Define `AnalysisStatus` ('processing', 'completed', 'failed').
    Define `AnalysisRun` interface: { id, subprojectId, timestamp, status, result?, parameters: { excludedEntryIds: string[] } }.
    Ensure results can store aggregate chart data (model-specific distributions).
    AVOID: Keeping this in the project service. Create a dedicated analysis domain.
  </action>
  <verify>Types compile without errors in the project</verify>
  <done>Analysis domain types are established</done>
</task>

<task type="auto">
  <name>Implement Analysis service with state persistence</name>
  <files>app/services/api/analysis/index.server.ts, app/services/api/mock/store.ts</files>
  <action>
    Implement `triggerProjectAnalysis(subprojectId, excludedEntryIds: string[])`.
    Mock the process duration (~5 seconds) and allow multiple concurrent runs.
    Implement `getSubprojectAnalysisHistory(subprojectId)` to return all runs.
    Update the mock store to hold these historical records.
    AVOID: Simple logic that blocks other runs. Use an array to store active and past tasks.
  </action>
  <verify>Mock service can return a list of history items for a subproject</verify>
  <done>Service layer handles concurrency and history persistence</done>
</task>

</tasks>

<verification>
 After all tasks, verify:
- [ ] Multiple analysis runs can be initiated for the same subproject.
- [ ] Each run has its own status and is recorded in history with its respective excluded IDs.
</verification>

<success_criteria>
- [ ] Analysis history service is fully functional and mocked.
</success_criteria>
