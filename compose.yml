services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # depends_on:
    #   - panoptic-api

  # panoptic-api:
  #   image: panoptic-api:latest
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - ENV_VAR=value
